{
  "name": "career-sync-root",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r dist ../client-build"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
